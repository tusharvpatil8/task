const appConfig = {
  apiPrefix: "/api",
  authenticatedEntryPath: "/blogs",
  unAuthenticatedEntryPath: "/sign-in",
  tourPath: "/",
  pagePerData: 10,
  enableMock: true,
};

export default appConfig;
